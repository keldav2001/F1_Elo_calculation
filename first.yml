info:
  name: first
  type: http
  seq: 1

http:
  method: GET
  url: https://api.jolpi.ca/ergast/f1/2024/13/results.json?round=1
  params:
    - name: round
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
